# UCP-001 — UCP Envelope, Command, Capabilities Types

| Field | Value |
|---|---|
| Task ID | UCP-001 |
| Owner | Protocol/Schema agent |
| Status | COMPLETE |
| Branch | agent/protocol/UCP-001-envelope |

## Goal
Hand-derive TypeScript types from the three JSON schemas in `schemas/` and
implement Zod runtime validation for each. Create fixture objects and Vitest
tests covering valid input and required-field rejection.

## Scope
- `packages/protocol/src/types.ts` — branded primitives
- `packages/protocol/src/envelope.ts` — UcpEnvelope interface
- `packages/protocol/src/command.ts` — UcpCommandPayload interface
- `packages/protocol/src/capabilities.ts` — UcpCapabilities interface
- `packages/protocol/src/validate.ts` — Zod schemas for all three
- `packages/protocol/src/index.ts` — re-exports
- `packages/protocol/src/fixtures/*.fixture.ts` — valid test objects
- `packages/protocol/src/__tests__/*.test.ts` — Vitest unit tests
- Root workspace: `package.json`, `pnpm-workspace.yaml`, `turbo.json`,
  `tsconfig.base.json`, `.nvmrc`, `.gitignore`

## Contracts consumed
- `schemas/ucp-envelope.schema.json`
- `schemas/ucp-command.schema.json`
- `schemas/ucp-capabilities.schema.json`

## Dependencies added
- `zod@^3.25.76` (runtime dep on @agent-deck/protocol)
- `vitest@^3.2.4` (devDep on @agent-deck/protocol)
- `typescript@^5.8.3`, `turbo@^2.5.4` (devDeps on root)

## Acceptance criteria met
- [x] All three schema shapes have TypeScript interfaces
- [x] Branded primitive types for ids/timestamps
- [x] Zod schemas match JSON schema constraints (min/max lengths, required fields, strict/passthrough)
- [x] Valid fixtures for all three shapes
- [x] Tests: valid input passes, missing required fields reject, invalid values reject
- [x] ESM-only, strict TypeScript, no cloud dependencies

## Tests not run
`pnpm install` was not executed (environment constraint). Vitest tests are
syntactically correct and rely only on zod + vitest, both declared as deps.
Run `pnpm install && pnpm --filter @agent-deck/protocol test` to execute.

## Handoff recipient
Bridge agent (BRG-001) — consumes @agent-deck/protocol types for the event journal and command ledger.
