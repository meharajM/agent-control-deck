{
  "name": "@agent-deck/qa-scenarios",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint: ok'"
  },
  "dependencies": {
    "@agent-deck/adapter-contract": "workspace:*",
    "@agent-deck/bridge-core": "workspace:*",
    "@agent-deck/bridge-database": "workspace:*",
    "@agent-deck/adapter-fake": "workspace:*",
    "@agent-deck/protocol": "workspace:*",
    "better-sqlite3": "^12.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/ws": "^8.5.13",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
